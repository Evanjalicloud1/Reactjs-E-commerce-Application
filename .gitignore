# Node / React junk (not needed since we only have build/, but safe to add)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Logs
*.log

# Build outputs (we only keep the given prebuilt build/ folder)
dist/
*.out

# OS files
.DS_Store
Thumbs.db

# Docker
*.pid
